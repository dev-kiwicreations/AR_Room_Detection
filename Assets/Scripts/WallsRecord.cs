using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WallsRecord : MonoBehaviour
{
    public List<Collider> wallColliders = new List<Collider>();
    public List<MeshRenderer> DoorMeshes = new List<MeshRenderer>();
    public List<MeshRenderer> WindowMeshes = new List<MeshRenderer>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
